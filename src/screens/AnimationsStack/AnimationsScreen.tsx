import {ScreenWrapper} from '@components';
import {Text} from 'react-native-paper';

export const AnimationsScreen = () => {
  return (
    <ScreenWrapper>
      <Text variant="titleLarge">This is testing field for Animations</Text>
    </ScreenWrapper>
  );
};
